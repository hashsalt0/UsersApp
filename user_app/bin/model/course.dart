import '../utils/config.dart';
/// The available courses for the user
/// The no of courses to enroll can be specifed thought [Config.minCourseEntry]
/// and [Config.maxCourseEntry]
enum Course {
  A, B, C, D, E, F
}